// this Wrapper class is essentially what React.Fragments automatically does for us. Wrote manually here as an example
const Wrapper = props => {
    return props.children;
};

export default Wrapper;